namespace first{
   int x = 1;
}

int main () {
	using namespace std;
	// using namespace first;
	int = 0;
	cout << first :: x; // output: 1
	return 0;
}

dieu kien ? lenh neu dung : lenh neu sai;

string s;
s.append();
s.lenght();
s.clear();
s.at(index);
s.insert(index,value);
s.find('char'); // output: index
s.erase(begin index,end index); // delete char in zone

srand(time(NULL));
int num = rand();
